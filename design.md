# Design Direction — Mahmoud Hisham Almodalal

## الفكرة الأساسية

صمّم الموقع كأنه **استوديو هندسي صغير له شخصية واضحة**، وليس صفحة مولدة من قالب تقني جاهز. الفكرة البصرية هي: **أنظمة حقيقية، مبنية بأيدٍ بشرية، وتعمل في العالم الحقيقي**.

يجب أن يشعر الزائر بأنه دخل إلى مساحة عمل مهندس يبني منتجات مفيدة، لا إلى لوحة تحكم مستقبلية أو صفحة تسويقية عامة. نستخدم لغة بصرية تحريرية، هادئة، دافئة، وفيها تفاصيل ملموسة مثل الورق، سطح المكتب، الصور الواقعية، الحواف غير المتطابقة تماماً، والملاحظات التقنية القصيرة.

> الهدف: إظهار الإبداع من خلال القرارات، التفاصيل، وطريقة عرض المشاريع؛ وليس من خلال المؤثرات الكثيرة.

## مبادئ التصميم

| المبدأ | التطبيق |
|---|---|
| **واقعي قبل أن يكون لامعاً** | استخدم صوراً فوتوغرافية أو موكابات لها إضاءة ومواد طبيعية. تجنب المشاهد المستقبلية والواجهات العائمة. |
| **تحريرية قبل أن تكون قالباً** | اعتمد مساحات كبيرة، عناوين واضحة، أعمدة غير متساوية، وفواصل تشبه صفحات المجلات والكتالوجات. |
| **الهندسة تظهر في التفاصيل** | أظهر التقنيات، طبقات النظام، نوع المشكلة، والقرار الهندسي داخل كل مشروع. لا تجعل التقنية مجرد قائمة شعارات. |
| **الهدوء يرفع الثقة** | عدد محدود من الألوان، حركات قصيرة، وظلال خفيفة. لا تستخدم gradient في كل عنصر. |
| **كل قسم له وظيفة** | يجب أن يجيب كل قسم عن سؤال واضح: من هو؟ ماذا يبني؟ كيف يعمل؟ ماذا أنجز؟ كيف أتواصل معه؟ |
| **الإنسان خلف النظام** | استخدم جمل شخصية دقيقة، صوراً فيها حياة، وتفاصيل من الرحلة التعليمية والمهنية دون مبالغة. |

## الهوية البصرية

### لوحة الألوان

استخدم الخلفية الداكنة كمساحة هادئة، ثم اجعل اللون البرتقالي يظهر فقط في نقاط القرار: الأزرار، الروابط الأساسية، مؤشرات الحالة، وخطوط التحديد.

| Token | القيمة | الاستخدام |
|---|---|---|
| `--bg` | `#10110F` | الخلفية الأساسية |
| `--bg-deep` | `#0C0D0B` | الفوتر وقسم التواصل |
| `--surface` | `#181A16` | أقسام المحتوى وبطاقات المشاريع |
| `--surface-raised` | `#20231D` | hover والبطاقات المرتفعة |
| `--border` | `#33372F` | الفواصل والحواف |
| `--text` | `#F4F1E9` | العناوين والنص الأساسي |
| `--muted` | `#AAA79C` | النصوص الثانوية |
| `--accent` | `#F17D4B` | الإجراء الأساسي والتمييز |
| `--accent-light` | `#F7C4A8` | الروابط والعناوين الدافئة |
| `--sage` | `#A9C7AB` | الحالات الإيجابية والحالة المتاحة |
| `--blue` | `#91A9CC` | إشارات البيانات أو AI عند الحاجة |

لا تستخدم أكثر من لون accent قوي داخل المشهد نفسه. البرتقالي هو اللون الرئيسي، والساج يستخدم كإشارة هادئة، والأزرق لا يظهر إلا عندما يشرح معنى تقنياً واضحاً.

### الخطوط

استخدم **Manrope** للعناوين والنصوص، و **DM Mono** للعناصر التقنية فقط مثل أسماء الملفات، التقنيات، الأرقام، والفلاتر. لا تستخدم الخط الأحادي للنصوص الطويلة؛ ذلك يجعل الصفحة تبدو كواجهة طرفية بدلاً من بورتفوليو إنساني.

العناوين يجب أن تكون كبيرة، ضيقة، وذات وزن مرتفع. أما النصوص الوصفية فتبقى قصيرة، ذات عرض محدود، وتستخدم line-height مريحاً. لا تجعل كل النصوص uppercase؛ استخدم الأحرف الكبيرة فقط للفلاتر والعناوين الصغيرة.

## بنية الصفحة

### 1. شريط التنقل

اجعل شريط التنقل عائماً بحواف مستديرة، شبه شفاف، ومثبتاً أعلى الصفحة. يجب أن يبدو كقطعة تحكم صغيرة لا كعنوان ضخم يأخذ مساحة من المحتوى.

أظهر الاسم المختصر يساراً وروابط الأقسام يميناً. على الهاتف يتحول إلى قائمة جانبية بسيطة. لا تضف أيقونات لكل رابط؛ النص وحده أكثر هدوءاً واحترافية.

### 2. القسم الافتتاحي

اجعل الـHero يجيب خلال خمس ثوانٍ عن ثلاثة أسئلة: من هو محمود؟ ما نوع الأنظمة التي يبنيها؟ وكيف يمكن التواصل معه؟

استخدم عنواناً من ثلاث وحدات واضحة:

```text
Backend
& AI
Engineer
```

ضع بجانبه بطاقة تقنية مستوحاة من ملف أو terminal حقيقي، لكن لا تجعلها واجهة خيالية. يجب أن تحتوي على معلومات بسيطة ومقنعة مثل الدور، مجال التركيز، التعليم، وحالة التوفر.

أضف مؤشرات مختصرة أسفل الوصف، مثل `Python / Django` و`SaaS & AI` و`91% GPA`. هذه المؤشرات تعمل كبيانات تعريفية سريعة وليست كإحصاءات تسويقية مبالغ فيها.

### 3. قسم المهارات

اعرض المهارات كـ **أدوات لبناء منتجات موثوقة** وليس كشبكة شعارات. كل بطاقة مهارة تحتوي على اسم واضح، سطرين أو ثلاثة من التقنيات، وتفصيل صغير يشرح طريقة الاستخدام عند الحاجة.

قسّمها إلى Backend، Databases، Frontend، DevOps، AI / ML، وArchitecture. حافظ على البطاقات هادئة، ذات حدود رقيقة، وhover بسيط يغير الخلفية أو يرفع البطاقة قليلاً.

### 4. قسم الخبرة

استخدم timeline رأسي واضحاً. التاريخ يبقى في عمود منفصل، والوظيفة والجهة والوصف في العمود الرئيسي. لا تضع مؤثرات كثيرة على الخط الزمني؛ نقطة برتقالية واحدة وخط رفيع يكفيان.

يجب أن تشرح أوصاف الخبرة ماذا تم بناؤه أو تحسينه، وليس فقط اسم الوظيفة. استخدم جملة واحدة أو اثنتين لكل تجربة، ثم tags قليلة للتقنيات المهمة.

### 5. المشاريع الرئيسية

ابدأ بثلاثة مشاريع فقط تحت عنوان **Systems built for real workflows**:

| المشروع | الرسالة التي يجب أن يوصلها |
|---|---|
| EduTracker | القدرة على بناء SaaS متعدد المستأجرين يعمل في بيئات ومدارس حقيقية. |
| Noor Al-Huda | القدرة على تصميم تجربة offline-first تخدم 300+ طالب وتستبدل السجلات الورقية. |
| clientMORE | القدرة على بناء منتج AI/RAG ثنائي اللغة يربط الأتمتة بالتصعيد البشري. |

كل بطاقة مشروع يجب أن تحتوي على موكاب خاص بالمشروع، العنوان، وصف من جملتين كحد أقصى، التقنيات الأساسية، ثم أزرار `Live demo` و`Repository` عند توفرهما.

لا تستخدم نفس صورة الموكاب لأكثر من مشروع. الاختلاف يجب أن يظهر في الشاشة، الألوان، المواد، زاوية التصوير، والأشياء الموجودة في المشهد.

### 6. المشاريع الإضافية

ضع المشاريع الداعمة تحت عنوان **Additional Projects** بفاصل واضح. هذه المنطقة ليست منافسة للمشاريع الرئيسية؛ هي أرشيف هندسي مختصر يثبت التنوع في computer vision، التجارة الإلكترونية، الأتمتة، C، Java، وأنظمة الإدارة.

لا تجعل بطاقات المشاريع الإضافية أكبر من البطاقات الرئيسية. استخدم صورة أقصر، وصفاً أكثر اختصاراً، ورابط GitHub واحداً فقط عندما لا يوجد Live Demo.

### 7. التعليم والشهادات

اعرض شهادة الهندسة كعنصر رئيسي، مع GPA `91%` كعلامة بصرية كبيرة ولكن شفافة في الخلفية. الشهادات الأخرى تظهر في شبكة صغيرة منظمة، وليس كقائمة طويلة من الروابط المتشابهة.

### 8. التواصل

اجعل قسم التواصل يشبه نهاية رسالة شخصية: عنوان مباشر، وصف قصير، وروابط بسيطة للبريد والهاتف وLinkedIn وGitHub وملفات CV وPortfolio. لا تستخدم نموذج تواصل غير ضروري ما لم يكن مربوطاً بخدمة حقيقية.

## أسلوب كتابة المحتوى

اكتب وصف كل مشروع وفق هذا الترتيب:

1. **المشكلة أو السياق:** ماذا كان يحتاج المستخدم أو الفريق؟
2. **الحل:** ماذا يفعل المنتج أو النظام؟
3. **التميّز الهندسي:** ما القرار التقني أو النتيجة التي تجعل المشروع مهماً؟

صيغة مناسبة:

> منصة ثنائية اللغة لإدارة عمليات المدرسة، توحّد الحضور والرسوم والمراسلات في نظام واحد، مع بنية متعددة المستأجرين ودعم المزامنة عند ضعف الاتصال.

تجنب الكلمات العامة مثل `modern` و`powerful` و`innovative` إذا لم تتبعها معلومة ملموسة. لا تقل إن النظام ثوري أو ذكي جداً؛ اشرح ماذا يفعل فعلياً.

## أسلوب الموكابات والصور

يجب أن تبدو الصور مثل جلسات تصوير تحريرية حقيقية، لا مثل صور مولدة لواجهة SaaS عامة. التزم بالتالي:

- أظهر لابتوباً وجوالاً في مشهد واحد عندما يكون المنتج responsive أو متعدد الأدوار.
- استخدم إضاءة طبيعية، ظلالاً حقيقية، انعكاسات خفيفة، ومواد ملموسة.
- اجعل النص داخل الشاشات صغيراً وغير محور الاهتمام؛ المحتوى الحقيقي يكتب في HTML خارج الصورة.
- اربط الأشياء المحيطة بالمشروع: ملفات مدرسية لـEduTracker، أوراق وتقارير لـNoor Al-Huda، مكتب دعم عملاء لـclientMORE، وterminal workstation لـMySellProject.
- لا تستخدم أشخاصاً بوجوه واضحة إلا إذا كان ذلك ضرورياً. الأيدي والأجهزة يجب أن تبدو طبيعية.
- لا تستخدم neon، holograms، cyberpunk، floating cards، glassmorphism مبالغاً فيه، أو خلفيات بنفسجية/زرقاء لامعة.

## الحركة والتفاعل

الحركة يجب أن تعطي إحساساً بالاستجابة لا بالاستعراض. استخدم transitions بين `160ms` و`260ms`، وحرّك `transform` و`opacity` فقط قدر الإمكان.

عند hover على بطاقة مشروع، يكفي تكبير الصورة بنسبة صغيرة ورفع البطاقة عدة pixels. أزرار Live Demo تتغير حدودها أو لونها، ولا تقفز أو تلمع بشكل مبالغ فيه.

احترم `prefers-reduced-motion`. يجب أن يبقى الموقع مفهوماً وسهل الاستخدام حتى مع إيقاف الحركة.

## الاستجابة وإمكانية الوصول

| العرض | القاعدة |
|---|---|
| واسع | ثلاث بطاقات مشاريع، عمودان أو ثلاثة للمهارات، وHero ثنائي الأعمدة. |
| متوسط | بطاقتان في الصف، تقليل المسافات، إبقاء النصوص ضمن عرض مريح. |
| هاتف | بطاقة واحدة في الصف، Hero عمودي، أزرار بعرض كامل، وقائمة تنقل جانبية. |

استخدم HTML دلالياً، نصوص `alt` دقيقة للصور، focus states واضحة، تبايناً كافياً، وروابط تفتح في تبويب جديد مع `rel="noopener"`.

## قائمة الممنوعات

لا تستخدم قوالب dashboard عامة، gradients متعددة الألوان، أيقونات ضخمة، نصوصاً تسويقية فارغة، صوراً متشابهة لكل المشاريع، أو موكابات تبدو كأنها concept art. لا تجعل كل شيء مستديراً أو زجاجياً. لا تضع عشرات التقنيات في كل بطاقة؛ اختر التقنيات التي تشرح قيمة المشروع فعلاً.

## معيار القبول

يُعتبر التصميم ناجحاً عندما يستطيع الزائر خلال أقل من دقيقة أن يفهم التخصص، يرى الفرق بين المشاريع، يميز المشاريع المبنية فعلياً من المشاريع التجريبية، ويفتح Live Demo أو Repository بسهولة.

قبل أي تعديل بصري لاحق، تحقق من أن التغيير يضيف شخصية أو وضوحاً أو ثقة. إذا كان التغيير مجرد مؤثر إضافي، فلا تضفه.
